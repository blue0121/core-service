<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.jutil.coreservice.admin.dao.ConfigMapper">

	<insert id="insertOne">
		INSERT IGNORE INTO adm_config (id, tenant_id, operator_id, code, name, value, status,
			remarks, create_time, update_time)
		VALUES (#{entity.id}, #{entity.tenantId}, #{entity.operatorId}, #{entity.code},
			#{entity.name}, #{entity.value}, #{entity.status}, #{entity.remarks},
			now(), now())
	</insert>

</mapper>